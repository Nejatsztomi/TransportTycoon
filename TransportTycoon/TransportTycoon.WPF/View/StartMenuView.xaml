<UserControl x:Class="TransportTycoon.WPF.View.StartMenuView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:TransportTycoon.WPF.ViewModel"
        xmlns:local="clr-namespace:TransportTycoon.WPF.View" d:DataContext="{d:DesignInstance Type=vm:StartMenuViewModel}"
        mc:Ignorable="d"
        Background="OrangeRed"
        >


    <UserControl.Resources>
        <Style x:Key="StarterButton" TargetType="Button">
            <Setter Property="FontFamily" Value="/Assets/Fonts/#Press Start 2P"/>
            <Setter Property="Background" Value="Orange"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="BorderThickness" Value="3"/>
            <Setter Property="BorderBrush" Value="Black"/>
            <Setter Property="MinWidth" Value="300"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Margin" Value="0 0 0 30"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
        </Style>
    </UserControl.Resources>

    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
        <TextBlock Text="TransportTycoon" FontSize="32" FontFamily="/Assets/Fonts/#Press Start 2P" Margin="0 0 0 50"></TextBlock>
        <Button Content="Create new game"
                Style="{StaticResource StarterButton}"
                Command="{Binding CreateNewGameCommand}" 
                ></Button>
        <Button Content="Start new game" 
                Command="{Binding NewGameCommand}"
                Style="{StaticResource StarterButton}">
        </Button>
        <Button Content="Load game" 
                Command="{Binding LoadGameCommand}"
                Style="{StaticResource StarterButton}">
            
        </Button>
        <Button Content="Exit Game" 
                Command="{Binding ExitGameCommand}" 
                Style="{StaticResource StarterButton}"
        ></Button>
    </StackPanel>
</UserControl>
