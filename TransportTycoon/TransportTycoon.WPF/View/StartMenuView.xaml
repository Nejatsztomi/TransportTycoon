<UserControl x:Class="TransportTycoon.WPF.View.StartMenuView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:TransportTycoon.WPF.ViewModel"
        xmlns:local="clr-namespace:TransportTycoon.WPF.View" d:DataContext="{d:DesignInstance Type=vm:StartMenuViewModel}"
        mc:Ignorable="d"
        >
    
    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
        <TextBlock Text="TransportTycoon" FontSize="30"></TextBlock>
        <Button Content="Create new game" Command="{Binding CreateNewGameCommand}"></Button>
        <Button Content="Start new game" Command="{Binding NewGameCommand}"></Button>
        <Button Content="Load game" Command="{Binding LoadGameCommand}"></Button>
        <Button Content="Exit Game" Command="{Binding ExitGameCommand}"></Button>
    </StackPanel>
</UserControl>
