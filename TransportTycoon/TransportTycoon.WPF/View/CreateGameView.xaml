<UserControl x:Class="TransportTycoon.WPF.View.CreateGameView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TransportTycoon.WPF.View"
             xmlns:viewmodel="clr-namespace:TransportTycoon.WPF.ViewModel" d:DataContext="{d:DesignInstance Type=viewmodel:CreateGameViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
        <TextBlock Text="Create new game" FontSize="30"></TextBlock>
        <TextBox Text="{Binding SaveName}" Width="200" Margin="0,10,0,10" HorizontalAlignment="Center" VerticalAlignment="Center"></TextBox>
        <TextBlock Text="Select Difficulty" VerticalAlignment="Center" HorizontalAlignment="Center"></TextBlock>
        <ComboBox SelectedIndex="{Binding SelectedDifficulty}">
            <ComboBoxItem Content="Easy Mode"></ComboBoxItem>
            <ComboBoxItem Content="Medium Mode"></ComboBoxItem>
            <ComboBoxItem Content="Hard Mode"></ComboBoxItem>
        </ComboBox>
        <Button Content="Create new game" Command="{Binding CreateNewGame}"></Button>
    </StackPanel>
</UserControl>
